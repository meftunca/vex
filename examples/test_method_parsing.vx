// Test: Method-level mutability parsing (immutable only for now)

struct Point {
    x: i32,
    y: i32,
    
    // Immutable method (no !)
    fn distance(): f32 {
        return 5.0;
    }
}

fn main(): i32 {
    let p = Point { x: 3, y: 4 };
    let d = p.distance();
    print("Distance:", d);
    return 0;
}
