// Test enum parsing

// Simple C-style enum
enum Color {
    Red,
    Green,
    Blue,
}

// Generic enum with data
enum Option<T> {
    Some(T),
    None,
}

// Enum with mixed variants
enum Result<T, E> {
    Ok(T),
    Err(E),
}

fn main(): i32 {
    // Simple C-style enum usage
    // For now, just return a tag value manually
    // Will add proper constructors next
    
    // Color::Red would be tag 0
    // Color::Green would be tag 1
    // Color::Blue would be tag 2
    
    return 1; // Green
}
