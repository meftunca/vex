// Test simple increment/decrement (postfix only for now)

fn main(): i32 {
    let! x = 10;
    
    // Post-increment
    let a = x++;  // a=10, x=11
    
    // Post-decrement  
    let b = x--;  // b=11, x=10
    
    return x + a + b;  // 10 + 10 + 11 = 31
}
