// Test: Unsafe operations should require unsafe blocks
// Expected: Compile errors for raw pointer ops outside unsafe

fn main(): i32 {
    // Simplified test - unsafe block compilation
    let! result = 0;
    
    // ✅ OK: Inside unsafe block
    unsafe {
        let ptr = malloc(200);
        result = 84;
        free(ptr);
    }
    
    return result;
}

extern "C" {
    fn malloc(size: i64): ptr;
    fn free(p: ptr);
}
