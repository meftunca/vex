// Native C library integration demo

extern "C" {
    fn add_numbers(a: i32, b: i32): i32;
    fn print_hello(name: *u8);
}

fn main(): i32 {
    // Call C function to add numbers
    let result: i32 = add_numbers(10, 32);
    
    // Note: String to C string conversion would need helper
    // For now, this is a simplified demo
    
    return result;
}
