// Test: Missing ! suffix error

struct Counter {
    value: i32,
    
    fn increment()! {
        self.value = self.value + 1;
    }
}

fn main(): i32 {
    let! counter = Counter { value: 0 };
    
    // ❌ ERROR: Should require !
    counter.increment();
    
    return 0;
}
