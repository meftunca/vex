// Minimal trait test

trait Show {
    fn show(self: &Self);
}

struct Num {
    val: i32
}

impl Show for Num {
    fn (self: &Num) show() {
        println(self.val);
    }
}

fn main() {
    let n = Num { val: 99 };
    n.show();
}
