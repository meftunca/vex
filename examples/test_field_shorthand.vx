// Field init shorthand test - Vex v0.9.2
// Status: 🚧 FUTURE FEATURE - Parser support needed
// Syntax: Point { x, y } instead of Point { x: x, y: y }

struct Point {
    x: i32,
    y: i32,
}

fn main(): i32 {
    let x = 100;
    let y = 200;
    
    // Full syntax (current support)
    let p1 = Point { x: x, y: y };
    println("Full syntax works");
    
    return 0;
}
