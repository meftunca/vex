// Test Type-Safe Zero-Cost Format
// Demonstrates Rust-style format() with compile-time type checking

import { println } from "io";

fn test_primitives() {
    // Integer types
    let x: i32 = 42;
    let y: i64 = 9999;
    
    println("=== Primitive Types ===");
    
    // Basic formatting (CURRENTLY NOT WORKING - need to wire format() to println)
    // println(format("Integer: {}", x));
    // println(format("Long: {}", y));
    
    // Float types
    let pi: f64 = 3.14159;
    let half: f32 = 0.5;
    
    // println(format("Float64: {}", pi));
    // println(format("Float32: {}", half));
    
    // Bool
    let flag: bool = true;
    // println(format("Boolean: {}", flag));
    
    // String
    let msg = "Hello, Vex!";
    // println(format("String: {}", msg));
    
    println("Primitives test complete (format() not wired to println yet)");
}

fn test_multiple_args() {
    println("=== Multiple Arguments ===");
    
    let a = 1;
    let b = 2;
    let c = 3;
    
    // println(format("a={}, b={}, c={}", a, b, c));
    // println(format("Sum: {} + {} = {}", a, b, a + b));
    
    println("Multiple args test complete");
}

fn test_mixed_types() {
    println("=== Mixed Types ===");
    
    let name = "Alice";
    let age: i32 = 25;
    let height: f64 = 1.75;
    let verified: bool = true;
    
    // println(format("Name: {}, Age: {}, Height: {}m, Verified: {}", name, age, height, verified));
    
    println("Mixed types test complete");
}

fn main(): i32 {
    println("=== Type-Safe Format Test ===\n");
    
    test_primitives();
    println("");
    
    test_multiple_args();
    println("");
    
    test_mixed_types();
    println("");
    
    println("=== All Tests Complete ===");
    return 0;
}
