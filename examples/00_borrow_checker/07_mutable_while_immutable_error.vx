// Borrow Rules Error: Immutable borrow while mutably borrowed

fn main() {
    let! x = 10;
    
    let r1 = &x!;  // mutable borrow
    let r2 = &x;   // ❌ ERROR: cannot borrow as immutable while mutably borrowed
    
    println(r1);
    println(r2);
}
