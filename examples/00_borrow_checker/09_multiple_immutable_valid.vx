// Borrow Rules Valid: Multiple immutable borrows

fn main() {
    let x = 10;
    
    let r1 = &x;  // immutable borrow
    let r2 = &x;  // ✅ OK: multiple immutable borrows are allowed
    let r3 = &x;  // ✅ OK
    
    println(r1);
    println(r2);
    println(r3);
}
