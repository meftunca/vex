// Factorial calculation in Vex

fn factorial(n: i32) : i32 {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

fn main() {
    let result: i32 = factorial(5);
    print(result);  // 120
}
