fn main() {
    // Test 1: Literal narrowing - should work (value known at compile time)
    let small: i8 = 127;  // ✅ i32 literal : i8 (safe, value is 127)
    let ubyte: u8 = 255;   // ✅ i32 literal : u8 (safe, value is 255)
    print("Test 1: Literal narrowing OK\n");
    
    // Test 2: Variable narrowing - should error
    let big: i64 = 1000;
    let truncated: i32 = big;  // ❌ Should error: variable downcast
    
    print("Should not reach here!\n");
}
