// Test fuzzy matching suggestions for typos

fn calculate_sum(x: i32, y: i32): i32 {
    return x + y;
}

fn main(): i32 {
    let message = "Hello";
    let counter = 42;
    let value = 100;
    
    // Typo in variable name - should suggest "message"
    println(mesage);
    
    // Typo in variable name - should suggest "counter"
    let result = cont + 10;
    
    // Typo in function name - should suggest "calculate_sum"
    let sum = calcualte_sum(5, 10);
    
    return 0;
}
