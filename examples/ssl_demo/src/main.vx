// OpenSSL SHA256 demo

extern "C" {
    fn SHA256(data: *u8, len: u64, output: *u8): *u8;
}

fn main(): i32 {
    // Simple test - hash computation would require proper buffer setup
    // This is a syntax demo showing how to declare OpenSSL functions
    return 0;
}
