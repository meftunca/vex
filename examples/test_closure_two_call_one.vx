// Two closures, call only first
fn main(): i32 {
    let add = |x, y| { x + y };
    let mul = |a, b| a * b;  // Created but not called
    
    let r1 = add(5, 10);
    if r1 != 15 { return 1; }
    
    return 0;
}
