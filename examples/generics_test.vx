// Test generic functions and structs

// Generic struct
struct Box<T> {
    value: T,
}

// Generic function
fn identity<T>(x: T): T {
    return x;
}

// Generic function with multiple type params
fn pair<T, U>(first: T, second: U): i32 {
    // For now, just return 0
    return 0;
}

fn main(): i32 {
    // Test generic function call with i32
    x := identity<i32>(42);
    
    // Test generic function call with f64
    y := identity<f64>(3.14);
    
    // Test generic struct instantiation with i32
    b1 := Box<i32> { value: 10 };
    
    // Test generic struct with different type (f64)
    b2 := Box<f64> { value: 3.14 };
    
    // Test pair function with mixed types
    p := pair<i32, f64>(100, 2.5);
    
    return x;
}
