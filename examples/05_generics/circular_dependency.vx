// Test: Circular generic dependency detection
// Expected: Compilation error (not stack overflow)

struct A<T> {
    value: T,
    b: B<T>
}

struct B<T> {
    value: T,
    a: A<T>
}

fn main(): i32 {
    // This should fail at compile time with circular dependency error
    // Not attempting to create instance - just checking if compiler detects cycle
    return 0;
}
