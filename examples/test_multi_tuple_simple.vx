// Test: Multi-Value Tuple Variant - Simple
// Test parsing and matching only (no constructor call yet)

enum IpAddr {
    V4(u8, u8, u8, u8),
    V6(string),
}

fn test_match(ip: IpAddr): i32 {
    match ip {
        IpAddr.V4(a, b, c, d) => {
            println("IPv4 matched");
            return 1;
        },
        IpAddr.V6(s) => {
            println("IPv6 matched");
            return 2;
        },
    };
    
    return 0;
}

fn main(): i32 {
    println("Multi-value tuple variant test");
    return 0;
}
