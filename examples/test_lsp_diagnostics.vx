// Test LSP real-time diagnostics
// Open this file in VSCode and type errors to see instant red squiggles

fn main(): i32 {
    // Syntax error test - remove semicolon
    let x = 42;
    
    // Immutability violation
    let y = 100;
    y = 200;  // ERROR: cannot assign to immutable
    
    // Use after move
    let s = "hello";
    let s2 = s;  // Move happens here
    print(s);    // ERROR: use of moved value
    
    return 0;
}
