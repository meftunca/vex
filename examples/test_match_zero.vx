// Debug tag value
import { Option } from "core";

extern "C" {
    fn printf(format: *u8, value: i32): i32;
}

fn main(): i32 {
    let result = Some(42);
    
    // For now, just test if pattern matching works at all
    let x = 0;
    let y = match x {
        0 => 100,
        _ => 200,
    };
    
    return y;
}
