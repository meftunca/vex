// Test operator overloading via builtin contracts
// Phase 1, Day 3-4

fn main(): i32 {
    // Test i32 + i32 (should use builtin Add contract)
    let a: i32 = 10;
    let b: i32 = 20;
    let sum = a + b;
    
    if sum == 30 {
        return 0; // Success
    } else {
        return 1; // Fail
    }
}
