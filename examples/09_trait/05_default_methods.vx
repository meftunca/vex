// Test trait methods (signature only - simplified syntax)
// Traits can only have method signatures, no body

trait Logger {
    // Simplified syntax - no explicit receiver
    fn log(msg: string);
    fn info(msg: string);
    fn debug(msg: string);
}

fn log2(msg: string) {
    // Call builtin print function
    print("LOG: ", msg);
    return;
}

struct ConsoleLogger impl Logger {
    prefix: string,
    
    // Implement all methods from trait (simplified syntax)
    fn log(msg: string) {
        log2(msg);
        return;
    }
    
    fn info(msg: string) {
        print("[INFO] ");
        self.log(msg);
        return;
    }
    
    fn debug(msg: string) {
        print("[DEBUG] ");
        self.log(msg);
        return;
    }
}

fn main(): i32 {
    let! logger = ConsoleLogger { prefix: ">>>" };
    
    logger.log("explicit");
    logger.info("info message");
    logger.debug("debug message");
    
    return 0;
}
