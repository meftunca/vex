// Test: contract keyword hala çalışıyor mu?

contract Logger {
   log(msg: string);
}

struct ConsoleLogger impl Logger {
    prefix: string,
    
   fn log(msg: string) {
        print(self.prefix, ": ", msg);
        return;
    }
}

fn main(): i32 {
    let logger = ConsoleLogger { prefix: "INFO" };
    logger.log("contract keyword works!");
    return 0;
}
