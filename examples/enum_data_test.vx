// Test data-carrying enum (simplified)

// For now, we'll test simple enums
// Data-carrying enums like Option<T> need:
// 1. Tagged union struct: { tag: i32, data: union {...} }
// 2. Constructor: Option_Some(T) -> creates struct
// 3. Pattern matching to extract data

// Simple test: just return enum tags
enum Result {
    Ok,
    Err,
}

fn main(): i32 {
    ok := Result_Ok();
    err := Result_Err();
    
    // Return Ok tag (0)
    return ok;
}
