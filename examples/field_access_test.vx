import "std";

// Simple test - just return if field access works
fn main() : i32 {
    // Create an error struct
    err := new_error("Something went wrong!");
    
    // Access the message field (should compile successfully)
    msg := err.message;
    
    // For now, just return 0
    // (In the future, we can print msg when printf is available)
    return 0;
}
