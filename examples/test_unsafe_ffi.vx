// Test: Unsafe block with FFI
extern "C" {
    fn printf(fmt: *u8, ...);
}

fn main(): i32 {
    unsafe {
        // Can call C functions in unsafe blocks
        let msg: String = "Hello from unsafe!\n";
        let ptr: *u8 = &msg as *u8;
        printf(ptr);
    }
    
    return 0;
}
