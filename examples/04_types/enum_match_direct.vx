// Direct enum return test

enum Status {
    Ok,
    Error,
}

fn main(): i32 {
    let s = Status.Ok;
    
    // Match expression returning value
    let result = match s {
        Status.Ok => 1,
        Status.Error => 3,
    };
    
    return result;
}
