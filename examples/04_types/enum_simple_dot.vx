// Minimal enum test with dot syntax

enum Color {
    Red,
    Green,
    Blue,
}

fn main(): i32 {
    // Create unit variant with dot syntax
    let c1 = Color.Red;
    let c2 = Color.Green;
    
    print("Color variants created");
    
    return 0;
}
