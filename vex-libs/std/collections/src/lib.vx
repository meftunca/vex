// std.collections - Collection types
// Re-exports all collection types and functions

// Import HashMap struct AND its static methods
import { HashMap, new_hashmap, with_capacity_hashmap } from "./hashmap";
import { HashSet, new_hashset, with_capacity_hashset } from "./hashset";

// Re-export everything from submodules to preserve static methods
export {
    HashMap,
HashSet
} from "./hashmap";

// Version info
const VERSION: str = "0.1.0";

fn version(): str {
    return VERSION;
}


