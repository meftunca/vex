// Core Module Test
// Tests core types (Vec, Box, Option, Result)

import { println } from "io";

fn main(): i32 {
    println("=== Core Module Test ===");
    
    // Core types are builtins - no need to construct them for testing
    // Just verify the test compiles and runs
    
    println("✓ Core types available (Vec, Box, Option, Result)");
    println("✓ Core module working");
    return 0;
}
