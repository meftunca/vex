// Builtin format() stub - compiler handles this specially
// DO NOT implement - just for type checking

// NOTE: Actual signature is variadic: format(fmt_str: String, ...)
// But parser doesn't support variadic in regular functions yet
// So we use a simple stub - compiler special-cases "format" anyway

export fn format(fmt_str: String): String {
    // This is never called - compiler replaces with compile_typesafe_format()
    return fmt_str;
}
