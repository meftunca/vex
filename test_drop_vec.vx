// Test automatic Drop trait (RAII) with Vec
fn main() {
    println("🧪 Testing Drop trait with Vec");
    
    // Vec should be automatically freed when scope ends
    {
        let! v = vec_new<i32>();
        vec_push(v, 10i32);
        vec_push(v, 20i32);
        vec_push(v, 30i32);
        println("Vec length: {}", vec_len(v));
    } // v.drop() should be called here automatically
    
    println("✅ Vec dropped automatically (check runtime logs)");
}
