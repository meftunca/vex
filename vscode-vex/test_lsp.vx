fn main(): i32 {
    let x = 42;
    let y = x + 10;
    let broken = "test";
    let z = 100;
    print(x);
    print(y);
    print(broken);
    print(z);
    return 0;
}

// Missing function body - syntax error (fixed)
fn test_func(): i32 {
    return 42;
}

// Valid struct
struct Point {
    x: i32,
    y: i32
}

fn print(x: Any) {
    // Builtin print function
}
