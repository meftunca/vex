// Test contract enforcement

// This should trigger a warning: public method without contract
export struct MyVec<T> {
    _data: *T,
    _len: usize,
    
    // ⚠️ WARNING: Public method 'push' not in any contract
    fn push(self: &MyVec<T>!, value: T) {
        // implementation
    }
    
    // ✅ OK: Private method (starts with _)
    fn _grow(self: &MyVec<T>!) {
        // internal helper
    }
}

fn main() {
    println(42);
}
