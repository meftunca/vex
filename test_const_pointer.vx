// Test const pointer syntax

extern "C" {
    // Const pointers (read-only)
    fn vex_hash(algo: *const u8, msg: *const u8, len: u64): i32;
    fn vex_print(text: *const u8, len: u64);
    
    // Mutable pointers (writable)
    fn vex_random_bytes(buf: *u8, len: u64): i32;
    fn vex_read_file(path: *const u8, out_buf: *u8, out_len: *u64): i32;
}

fn main(): i32 {
    return 0;
}
