// Test contract prelude - should have contracts available without import

// Should work without explicit import (contracts in prelude)
export struct MyVec<T> impl Collection<T>, Stack<T> {
    _data: *T,
    _len: usize,
    _capacity: usize,
    
    // Collection contract
    fn len(self: &MyVec<T>): usize {
        return self._len;
    }
    
    fn is_empty(self: &MyVec<T>): bool {
        return self._len == 0;
    }
    
    fn clear(self: &MyVec<T>!) {
        self._len = 0;
    }
    
    // Stack contract
    fn push(self: &MyVec<T>!, value: T) {
        // implementation
    }
    
    fn pop(self: &MyVec<T>!): Option<T> {
        return Option.None;
    }
    
    fn peek(self: &MyVec<T>): Option<&T> {
        return Option.None;
    }
    
    // Private helper - no contract needed
    fn _grow(self: &MyVec<T>!) {
        // internal
    }
}

fn main() {
    println(42);
}
